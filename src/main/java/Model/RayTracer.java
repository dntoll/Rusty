package Model;

public class RayTracer {
    public RayTracer(Level level) {
    }

    public RayWorldContact getTrace(Observer o, double fov, int left, int numRays) {
        return new RayWorldContact();
    }
}
